server:
  port: 8080

spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        default_schema: wateradventure
    show-sql: true
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:postgresql://localhost:5432/wateradventure
    username: postgres
    password: qwerty
  sql:
    init:
      mode: always
      continue-on-error: true
  flyway:
    baseline-on-migrate: true
    url: jdbc:postgresql://localhost:5432/wateradventure
    default-schema: wateradventure
    user: postgres
    password: qwerty